<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=0.8">
    <title>ChatRemind - 로그인</title>
    <link rel="stylesheet" href="public/styleslogin.css">
</head>
<body>
    <div class="layout">
        <a href="/"><button id="logo">ChatRemind</button></a>
    </div>
</body>
<body>
    <div class="login-container">
        <form action="/auth/login" method="post">
            <input id="typing-container" type="text" name="email" placeholder="아이디" required autofocus>
            <input id="typing-container" type="password" name="password" placeholder="비밀번호">
            <% if (typeof messages !== 'undefined' && messages.loginError) { %>
                <div class="error-message"><%= messages.loginError %></div>
            <% } %>
            <button id="login-button" type="submit">로그인</button>
        </form>
        <a href="/join"><button id="join-button">회원가입</button></a>
    </div>
<script>

</script>
</body>
</html>
