<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=0.8">
    <title>ChatRemind - 계정삭제</title>
    <link rel="stylesheet" href="public/styleslogin.css">
</head>
<body>
    <div class="layout">
        <a href="/"><button id="logo">ChatRemind</button></a>
    </div>
</body>
<body>
    <div class="login-container">
        <form action="/auth/withdrawal" method="post" onsubmit="return confirm('정말로 탈퇴하시겠습니까?');">
            <input id="typing-container" type="password" name="present_pw" placeholder="비밀번호" required autofocus>
            <% if (typeof messages !== 'undefined' && messages.joinError) { %>
                <div class="error-message"><%= messages.joinError %></div>
            <% } %>
            <button id="joinsubmit-button" class="btn" type="submit">탈퇴하기</button>
        </form>
        <a href="/mypage"><button id="join-button">이전으로</button></a>
    </div>
</body>
</html>